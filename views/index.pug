extends layout

block content
  // Hero
  section(class="py-12")
    .mx-auto.max-w-3xl.flex.flex-col.items-center.text-center.gap-6
      img(src="/static/img/gitpic3.jpg", alt="Terrance Ford", class="w-28 h-28 rounded-full ring-2 ring-gray-300 dark:ring-gray-700")
      h1(class="text-4xl font-extrabold tracking-tight") Welcome!
      p(class="text-lg text-gray-600 dark:text-gray-300")
        |  My name is a Terrance Ford and I'm an IT professional based in Raleigh, North Carolina. The projects below use various technologies such as React.js, Vanilla Javascript, HTML, CSS, PostgreSQL, Node.js, and several others. More simple apps are hosted on Github pages as others are hosted on #[a(href="https://www.netlify.com/"  target="_blank") #{"Netlify"}] for the front-end, #[a(href="https://www.heroku.com/?utm_source=microsoft&utm_medium=paid_search&utm_campaign=amer_heraw&utm_content=general-branded-search-rsa&utm_term=Heroku&utm_source_platform=microsoftAds&msclkid=fa08530302e4144740f734c1354ef679"  target="_blank") #{"Heroku"}] for the back-end, and #[a(href="https://neon.tech/" target="_blank") #{"Neon"}] for the database.

      .mt-2
        a(href="/about", class="inline-block px-5 py-3 rounded-xl border border-gray-300 dark:border-gray-700 hover:bg-gray-100 dark:hover:bg-gray-800") Learn more

  // Projects
  section(class="mt-16")
    h2(class="text-2xl font-semibold mb-6 text-center") Featured Projects
    div(class="grid sm:grid-cols-2 lg:grid-cols-3 gap-6")
      each project in projects
        a(href=`/projects/${project.id}`, class="block bg-gray-50 dark:bg-gray-800 rounded-2xl p-5 shadow hover:shadow-lg transition-shadow")
          if Array.isArray(project.image_url) && project.image_url.length
            img(src='/' + project.image_url[0], alt=project.project_name, class="w-full h-40 object-cover rounded-xl mb-4")
          else if typeof project.image_url === 'string'
            img(src='/' + project.image_url, alt=project.project_name, class="w-full h-40 object-cover rounded-xl mb-4")
          h3(class="text-lg font-semibold mb-1")= project.project_name
          if project.description
            p(class="text-sm text-gray-600 dark:text-gray-300 line-clamp-3")= project.description
